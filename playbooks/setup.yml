---
# =============================================================================
# Полная раскатка сервера: common → docker → tailscale → firewall → swarm
# =============================================================================
- name: Полная настройка серверов
  hosts: all
  become: true
  roles:
    - common
    - docker
    - tailscale
    - firewall
    - swarm
